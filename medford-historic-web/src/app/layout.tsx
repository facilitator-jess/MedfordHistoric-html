import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Medford Historic Properties",
  description: "Explore historic properties in Medford, Massachusetts. Browse architectural styles, construction dates, and historical information for over 30 properties.",
  keywords: "Medford, Massachusetts, historic properties, architecture, historical commission",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
